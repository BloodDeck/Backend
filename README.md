# BloodDeck Backend

This is the Django-based backend for the BloodDeck project.

## Project Structure
* **blooddeck/**: Project configuration and settings.
* **api/**: Main application logic and API endpoints.

## Setup Instructions

### 1. Environment Setup
Create a virtual environment and install Django:
```bash
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
pip install django